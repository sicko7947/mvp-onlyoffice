# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
document-main
web-apps-master
sdkjs-master
# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# Claude Code local config
.claude/

# stray bun lockfile — repo standardizes on pnpm (pnpm-lock.yaml)
bun.lock

# typescript
*.tsbuildinfo
next-env.d.ts

# OnlyOffice runtime asset bundles (1GB+, regenerable build artifacts).
# Produced from upstream DocumentServer + x2t WASM by scripts/onlyoffice-build/
# (see its README). Not vendored in git — run the pipeline to materialize them.
/public/packages/onlyoffice/7/
/public/packages/onlyoffice/9/
